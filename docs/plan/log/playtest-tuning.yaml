# Phase log — playtest-tuning
# Each entry: { task, story, description, result, notes, tokens }

entries:
  - task: 1
    story: US-34
    description: "Investigation — current balance values, test references, GameScene config usage"
    result: pass
    notes: "Water Pistol damage 25→15, Block Tower cost 50→25 health 400→90, Mine recharge 30000→20000, MINE_ARM_DELAY 10000→6000. Tests use hardcoded fixtures (Combat) or import constants (SingleUseDefenders) — all should pass after config changes. Wall health 90 > others' 40, passes Defenders.test relative check."
    tokens: ~5k
  - task: 2
    story: US-34
    description: "Apply balance changes — Water Pistol damage 25→15, Block Tower cost 50→25 health 400→90, Mine rechargeTime 30000→20000, MINE_ARM_DELAY 10000→6000"
    result: pass
    notes: "Config-only changes to defenders.ts. All 117 tests pass. ✓ US-34 complete"
    tokens: ~3k
  - task: 3
    story: US-32
    description: "Investigation — spark spawn interval, click zone, visual shape in GameScene.spawnSpark"
    result: pass
    notes: "SPARK_SPAWN_INTERVAL=8000 L39→4000. Zone 24x24 L645→48x48. Diamond shape: outer glow r=12→22, diamond ±8/±6→±16/±12, center r=3→5. Already has 3-layer shape (glow+diamond+spot). Queued implement."
    tokens: ~3k
  - task: 4
    story: US-32
    description: "Implement spark economy boost — SPARK_SPAWN_INTERVAL 8000→4000, click zone 24→48, scale diamond shape"
    result: pass
    notes: "Multi-layer shape (glow+ring+diamond+star cross+center). 117 tests pass, no regressions. ✓ US-32 complete"
    tokens: ~3k
  - task: 5
    story: US-33
    description: "Investigation — generator income flow, spark spawn, playProduce, test impacts for JitB rework"
    result: pass
    notes: "tickGeneratorIncome L127 loops defenders with generatesIncome>0, adds income, calls playProduce. Need: set generatesIncome=0, spawn clickable spark at defender position instead, expire after 5s. Test fix: Defenders.test L26 generatesIncome>0→toBe(0). New test needed: generator+time≠balance increase. GENERATOR_INCOME_INTERVAL=5000 stays as spawn interval."
    tokens: ~4k
  - task: 6
    story: US-33
    description: "Implement JitB rework — generatesIncome→0, spawn clickable sparks at defender position, 5s expiry"
    result: pass
    notes: "tickGeneratorIncome now checks behavior=generator, spawns spark at defender position with random offset, 5s auto-expiry. Updated Defenders.test, added passive-income-zero test. 118 tests pass, no regressions. ✓ US-33 complete"
    tokens: ~5k
